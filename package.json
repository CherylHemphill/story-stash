{
  "name": "story-stash",
  "version": "1.0.0",
  "description": "Search it, find it, read it, love it.",
  "main": "server.js",
  "scripts": {
    "start": "node server/server.js",
    "develop": "concurrently \"cd server && npm run watch\" \"cd client && npm start\"",
    "install": "cd server && npm i && cd ../client && npm i",
    "build": "cd client && npm run build"
  },
  "keywords": [],
  "author": "CherylHemphill",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^5.1.0",
    "nodemon": "^2.0.3"
  },
  "dependencies": {
    "apollo-server": "^3.12.0",
    "graphql": "^16.8.0"
  }
}
